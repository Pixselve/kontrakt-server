# Migration `20200417155902`

This migration has been generated by Pixselve at 4/17/2020, 3:59:02 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TYPE "Mark" ADD VALUE 'BLUE';
ALTER TYPE "Mark" ADD VALUE 'TODO';
ALTER TYPE "Mark" ADD VALUE 'TO_FINISH'
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200410134653..20200417155902
--- datamodel.dml
+++ datamodel.dml
@@ -3,14 +3,14 @@
 }
 generator typegraphql {
   provider = "node_modules/typegraphql-prisma/generator.js"
-  output   = "../prisma/generated/type-graphql"
+  output   = "../src/generated/type-graphql"
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model Contract {
   date DateTime
@@ -19,13 +19,13 @@
 }
 model SkillToStudent {
   id        Int                       @default(autoincrement()) @id
-  mark      Mark
+  mark      Mark @default(TODO)
   skillId   Int
   studentId Int
   skill    Skill                   @relation(fields: [skillId], references: [id])
-  student  Students                 @relation(fields: [studentId], references: [id])
+  student  Student                 @relation(fields: [studentId], references: [id])
 }
 model Skill {
   id              Int               @default(autoincrement()) @id
@@ -34,9 +34,9 @@
   contract Contract @relation(fields: [contractId], references: [id])
   contractId Int
 }
-model Students {
+model Student {
   firstName       String
   id              Int               @default(autoincrement()) @id
   lastName        String
   skillToStudents SkillToStudent[]
@@ -52,5 +52,8 @@
   GREEN
   MARKED
   ORANGE
   RED
+  BLUE
+  TODO
+  TO_FINISH
 }
```


